// import '@duetds/components';
// import '@upradata/stencil-flex';

/* import { LAYOUT_BREAKPOINTS, MediaQuery, BreakpointObserver, isMobile, AddResponsiveClasses } from '../responsive';

const breakpointObserver = new BreakpointObserver();
const mediaQuery = new MediaQuery(breakpointObserver, LAYOUT_BREAKPOINTS);

export const mt = {
    isMobile,
    LAYOUT_BREAKPOINTS,
    MediaQuery,
    BreakpointObserver,
    AddResponsiveClasses,
    services: {
        mediaQuery: new MediaQuery(breakpointObserver, LAYOUT_BREAKPOINTS),
        breakpointObserver,
        addResponsiveClasses: new AddResponsiveClasses({ isMobile, mediaQuery, }, document.body)
    }
};


export type MT = typeof mt;

window.mt = { ...(window.mt || {}), ...mt };
 */
